[build]
builder = "nixpacks"

[deploy]
startCommand = "python relay_server.py --host 0.0.0.0 --port $PORT --no-ws"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
